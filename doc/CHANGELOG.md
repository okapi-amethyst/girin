# CHANGELOG

> **このドキュメントの役割:** 設計・実装上の変更履歴と、検討事項が解決した際の経緯を記録する。
> AIに読ませる設計書ではなく、人間が参照する備忘録。
> 優先度・実装時期が未定の機能は `実装未定.md` を参照。

---

## 2026-04-16

### データスキーマ
- 変更内容: `creator_name` フィールドを削除
- 理由: 投稿フォームで入力させるのも目立つのも良くないという判断。考案者への言及が必要な場合は `notes` に記述する
- 影響範囲: `コンボ1枚フォーマット.md` / `コンボカード・フィルタ.md` / `コンボ投稿ページ.md` / `sample-combos.ts`（型・データ）/ `ComboCard.astro`

---

## 2026-04-15

### コンボ投稿ページ Phase 1 方針変更の検討開始

- 変更内容: 従来「Phase 1 は投稿ページなし（404）」だった方針を見直し、管理者1人が自身の上達用コンボをサンプルデータに追加するための簡易投稿UIを Phase 1 から提供する方向で検討を開始
- 理由: 管理者（プロジェクトオーナー）が自身のコンボをサイト上で表示・練習したい要望が発生したため
- 追加ファイル: `01_リサーチ/コンボ投稿ページリサーチ.md`（2XKOMBO / ComboForge の投稿フォーム比較と GIRIN 想定フォームとの差分整理）
- 追加ファイル: `01_リサーチ/コンボ投稿ページリサーチ・ボタン入力UI案.md`（ボタンクリックで入力表記を生成する仕組みの設計案）
- 変更ファイル: `03_詳細設計/コンボ投稿ページ.md`（Phase 1 「未実装」→ 管理者専用簡易実装として設計。フィールド一覧・ビジュアルボタンUI概略・JSON出力方式・段階実装表を追記）

### タグ追加: BNB / TOD

- 変更内容: 追加要素タグ（`tags`）に `bnb`（BNB）と `tod`（TOD）を追加。合計10→12タグ
- 理由: 2XKOMBO・ComboForge 両サイトのリサーチで共通して使われており、既存フィールド（position/difficulty/starter）では代替できない概念であるため。自由記述タグではなく定義済みチップとして追加することで一貫性を保つ
- 変更ファイル: `02_企画設計/コンボ1枚フォーマット.md`、`03_詳細設計/コンボカード・フィルタ.md`

---

## 2026-04-12

### My Combos / 新着コンボ ペア表示方針確定

- 変更内容: My Combos Phase 2 はペアを先に選択するUIを設ける。選択後は同一ペアのコンボのみ表示するため ComboCard へのペアラベル追加は不要
- 変更内容: 新着コンボ Phase 2 はページ側でペアごとにグループ化し、グループヘッダー行（キャラアイコン + ペア名）で区切る方式を採用。ComboCard 自体は拡張しない
- 変更ファイル: `03_詳細設計/My Combosページ.md`、`03_詳細設計/新着コンボページ.md`

### コンボ単体ページ仕様変更

- 変更内容: `section.related-combos`（関連コンボ一覧）を削除。コンボ単体ページは「練習に集中する」ページであり、他コンボへの誘導は不要と判断
- 変更内容: 広告配置の候補位置を ①combo-info後 / ②share-actions後（フッター直前）の2候補に整理（③related-combos下を削除）
- 変更ファイル: `03_詳細設計/コンボ単体ページ.md`、`02_企画設計/サイト構造設計.md`

### パンくずナビ仕様確定

- 変更内容: 全ページのパンくず段数を確定。Fuse別=3段（トップ/ペア名/Fuse名）、ペアトップ=2段（トップ/ペア名）、Juggernaut=3段（トップ/キャラ名/JUGGERNAUT）、キャラ単体=2段。段数が異なるのは親ページの構造が違うため意図的
- 変更内容: `ナビ・ヘッダー・フッター.md` のパンくず一覧表を正しい仕様に更新し、解決済みの課題「パンくずの不統一」を削除
- 変更ファイル: `03_詳細設計/ナビ・ヘッダー・フッター.md`

### フィルタUI仕様

- 変更内容: URLクエリ同期（`history.replaceState` / `applyQueryState()`）を **Fuse別ページ専用** と確定。Juggernaut・My Combos・新着コンボには実装しない方針
- 変更ファイル: `03_詳細設計/コンボカード・フィルタ.md`（URL クエリ同期セクションに専用明記）、`03_詳細設計/Juggernautページ.md`（課題セクションを「URLクエリ同期は実装しない」に更新）

### フェーズ定義の変更

- 変更内容: Phase 1 の位置づけを「公開リリース」から「非公開の見た目・機能確認用プレビュー」に変更。Phase 2 を正式な公開リリースと定義
- 変更内容: Phase 2 移行基準を「1人管理が回らなくなったとき」→「見た目・機能が完成したと自分が判断したとき」に変更
- 変更内容: `数値指標・Phase移行基準.md` を再構成。旧「Phase移行・拡張の判断基準」表はPhase 2→3 の基準として残し、Phase 1→2 の基準を「UI完成度・コンボ数・OGP・Bot動作・スマホ対応」の確認観点ベースに変更。「コンボ総数200超でDB化」は不要になったため削除
- 変更ファイル: `PROJECT_PROMPT.md`、`02_企画設計/数値指標・Phase移行基準.md`

### 広告配置設計

- 変更内容: My Combos・新着コンボ・コンボ投稿の広告配置を詳細設計に追記。3ページとも Phase 1 は広告なし、Phase 2 以降の方針を確定
  - 新着コンボ: ページング下 300×250 最優先、フィルターバー上 320×50 を段階導入候補
  - My Combos: ページング下 300×250 のみ。コンボ0件時は広告非表示。noindex のため自動広告無効化
  - コンボ投稿: 原則広告なし（フォームページ）。送信完了後エリアのみ例外検討
- 変更内容: `02_企画設計/サイト構造設計.md` のページ別サマリー表の「未定」行を更新
- 変更ファイル: `03_詳細設計/新着コンボページ.md`、`03_詳細設計/My Combosページ.md`、`03_詳細設計/コンボ投稿ページ.md`、`02_企画設計/サイト構造設計.md`

### SEO・OGP設計

- 変更内容: 全ページのSEO/OGP仕様（タイトル・メタディスクリプション・OG画像・canonical・noindex方針）を各詳細設計書に追記
- 変更内容: `02_企画設計/サイト構造設計.md` のSEOセクションを全ページ対応のサマリー表に更新
- noindex方針確定: My Combosは常時noindex / 新着コンボはPhase 1のみnoindex
- OG画像方針確定: ペア・Fuse別・コンボ単体は動的生成、それ以外は共通画像1枚
- 変更ファイル: `03_詳細設計/トップページ.md`、`03_詳細設計/キャラ単体ページ.md`、`03_詳細設計/ペアトップページ.md`、`03_詳細設計/Fuse別ページ.md`、`03_詳細設計/Juggernautページ.md`、`03_詳細設計/新着コンボページ.md`、`03_詳細設計/My Combosページ.md`、`03_詳細設計/コンボ投稿ページ.md`、`02_企画設計/サイト構造設計.md`

### データスキーマ・ソート仕様

- 変更内容: `practicality`（実用度スコア）フィールドをデータスキーマ・フィルター・コンボカードからすべて削除
- 理由: 算出ロジックが独自すぎてユーザーの納得感が得られるか未検証。Phase 1 のデフォルトソートとして使うには時期尚早と判断し廃止。将来実装は `実装未定.md` に移動
- 変更内容: デフォルトソートを `practicality-desc` → `difficulty-asc` に変更
- 変更ファイル: `src/data/sample-combos.ts`、`src/scripts/pair-combo-page.ts`、`src/components/ComboCard.astro`、`src/components/PairComboPage.astro`、`src/pages/[slug]/juggernaut.astro`、`02_企画設計/コンボ1枚フォーマット.md`、`02_企画設計/サイト構造設計.md`、`03_詳細設計/コンボカード・フィルタ.md`、`03_詳細設計/Fuse別ページ.md`、`03_詳細設計/Juggernautページ.md`、`03_詳細設計/コンボ単体ページ.md`、`04_制作・実装/Webサイト実装進捗.md`

- 変更内容: `target_characters` フィールドの型を `object` → `enum` に変更。`all`（制限なし）/ `except`（一部キャラ不可）/ `only`（キャラ限）の3値に簡略化。制限詳細は投稿時に `notes` へ記入する運用とし、投稿フォームにセレクトUIを追加
- 理由: Object 型は投稿者負担が大きい。3値の選択肢 + 注釈テキストで十分な情報が得られると判断
- 変更内容: `related_combo_id` フィールドを削除。パルスあり/なし版のコンボを相互参照する用途だったが、需要が低く管理コストが高いため廃止
- 変更ファイル: `02_企画設計/コンボ1枚フォーマット.md`、`03_詳細設計/コンボ投稿ページ.md`

---

## 2026-04-11

### sidekick-alt 廃止・ポイントキャラフィルター追加

- 変更内容: `sidekick-alt` URL を廃止。Sidekick コンボをすべて `/sidekick/` に統合し、始動方向は `point_character` フィールド＋ポイントキャラフィルターで区別する設計に変更
- 変更内容: 全ペアFuseページのフィルターバー常時表示行に「ポイントキャラ」フィルターを追加（チップ: すべて / キャラA / キャラB。Juggernaut は非表示）
- 理由: URL で始動方向を分けると `point_character` フィールドとの二重管理になる。フィルターで区別すれば1URLに統合でき、Double Down 等の他Fuseでも同じフィルターが使えて設計が統一される
- 変更ファイル: `02_企画設計/サイト構造設計.md`、`02_企画設計/コンボ1枚フォーマット.md`（point_character をフィルター対象に昇格・fuse 選択肢に廃止注記）、`03_詳細設計/コンボカード・フィルタ.md`（data-point-character 追加・フィルターバー共通仕様追記）、`03_詳細設計/Fuse別ページ.md`（フィルターバーに追記）

### Fuse タブナビの Sidekick リンク仕様を確定

- 変更内容: Fuse タブナビの Sidekick 2つは両方とも `/sidekick/` を参照し、`?point=[charId]` クエリパラメータでポイントキャラフィルタを初期適用する設計に確定（案A）
- 変更内容: タブ切替時のフィルタ引き継ぎで `point` パラメータはタブ自身の値で上書きする（他フィルタは引き継ぐ）
- 変更内容: アクティブ判定はパス `/sidekick/` かつ `?point` 値一致のタブを強調
- 理由: 2つのタブで URL パスを分けると sidekick-alt 廃止と矛盾する。クエリパラメータで初期フィルタを差し込めばパスを統一しつつ始動方向を即時適用できる
- 変更ファイル: `03_詳細設計/Fuse別ページ.md`（Fuseタブナビセクション改訂）

### 逆順ペア URL の扱いを確定

- 変更内容: 逆順ペア URL（`/yasuo/ahri/` 等）はリダイレクトせず 404 とする方針に確定
- 理由: 内部ナビゲーションと Discord Bot が常に正順 URL を生成するため逆順 URL がユーザーに届く経路がない。Astro SSG でのリダイレクト実装コストに見合わない
- 変更ファイル: `02_企画設計/サイト構造設計.md`、`03_詳細設計/キャラ単体ページ.md`

### Headroom ナビを全ページ共通に変更

- 変更内容: Headroom ナビの適用範囲を「Fuse別ページのみ」→「全ページ共通」に変更。スタイル・構造の標準は Fuse別ページの実装に統一
- 理由: トップ・キャラ単体など高さが出にくいページでは実質表示されないが、全ページに実装しておく方針に統一
- 変更ファイル: `03_詳細設計/ナビ・ヘッダー・フッター.md`（セクション名・説明を修正）、`03_詳細設計/ペアトップページ.md`（headroom-nav を追加、トリガー定義）

### 収益化設計の方針変更

- 変更内容: 収益化手段を「広告・寄付・アフィリエイトの3手段のみ」に絞り込み。プレミアム機能（有料サブスク）・Tier制・スポンサー制度・API提供・グッズ販売をすべて撤廃
- 理由: やりすぎ。収益化の目的はインフラ支出を賄うことだけで十分

- 変更内容: ドキュメントの構成を「収入予想重視」→「支出起点（支出を賄うために必要な最低収入の算出）」に全面改訂。Phase別の支出目安から逆算して必要PV・寄付件数を明示
- 変更ファイル: `02_企画設計/収益化設計.md`（全面改訂）、`PROJECT_PROMPT.md`（収益化表を修正）

### ペアトップページの設計確定

- 変更内容: ペアトップページの設計を「Fuseタブ選択ハブ + 最新コンボ5件プレビュー」に確定。フィルターバー・Headroomナビ・ページングなし。ALL タブも廃止
- 理由: コンボ全件一覧よりも Fuse 選択への誘導を優先するシンプルな構成に。最新5件はページのコンテンツとして十分で、詳細は各 Fuse 別ページに委ねる
- 変更ファイル: `03_詳細設計/ペアトップページ.md`（全面改訂）、`02_企画設計/サイト構造設計.md`（URL行の「要検討」を解消）

### Discordロール自動付与を実装未定に移動

- 変更内容: Discord Bot設計.md のロール設計セクションを `実装未定.md` に移動。ターゲットユーザー.md の具体的ロール名を「実装未定」注記に置き換え
- 理由: リリース時点ではコミュニティに実績がなくロールに箔がない。LocalStorage（Phase 2）でのクリアデータ連携方法も未確定なため、コミュニティが育ってから実装する方針に変更

### コンボ投稿窓口の一本化

- 変更内容: コンボ投稿の受付窓口を「Webフォーム（`/submit/`）のみ」に一本化。Discord `#コンボ投稿` チャンネルを廃止し `#コンボ相談` に統合。Bot `/submit` コマンドの設計を撤廃
- 理由: コンボデータのフィールド数が多く、Discord のやり取りで投稿を受け付けるには情報量が多すぎる。フォームによる構造化入力が必要

- 変更内容: Phase 1 は管理者が直接 JSON で追加（公開投稿窓口なし）を明記。Phase 2 の Webフォームが唯一の投稿経路
- 変更ファイル: `PROJECT_PROMPT.md`、`Discordサーバー設計.md`、`サイト構造設計.md`

### Phase定義 / Bot設計 / My Combos

- 変更内容: `PROJECT_PROMPT.md` の My Combos 行を「Phase 1は Coming Soon」→「Phase 1はサンプルデータでUI実装。Phase 2でLocalStorage実装」に変更
- 理由: Phase 1 はリリースではなくモック段階。My Combos のUI実装自体はサンプルデータで Phase 1 に実施し、LocalStorage による実機能は Phase 2 とすることで整合させた

- 変更内容: `Discord Bot設計.md` の `/mycombo` の Phase を 1 → 2 に修正。開発フェーズ表を整備（Phase 3 を追記、不完全な記述を修正）
- 理由: `/mycombo` は LocalStorage に保存したコンボを参照する導線であり、LocalStorage が Phase 2 実装なので Phase 1 に置く意味がない。PROJECT_PROMPT.md の Phase 2 記載が正しかった

- 変更内容: `PROJECT_PROMPT.md` の `PROMPT_COMBO_DATA.md` への参照（冒頭・参照表）を `02_企画設計/コンボ1枚フォーマット.md` に変更
- 理由: PROMPT_COMBO_DATA.md の廃止（同日）に伴う残存参照の修正

### データスキーマ / 設計ドキュメント整理

- 変更内容: `PROMPT_COMBO_DATA.md` を廃止。`コンボ1枚フォーマット.md` の参照行を削除
- 理由: スキーマ定義が `コンボ1枚フォーマット.md` に一本化されており、`PROMPT_COMBO_DATA.md` は重複かつ古いスキーマのまま残存していた。複数の真実が存在する状態を解消するために廃止

- 変更内容: `コンボカード・フィルタ.md` の `data-starter` 属性値から `throw` / `counter_hit` を削除（`ground / anti_air / pressure_mixup / tag_launcher` の4値に修正）。タグ件数表記を「全8タグ」→「全10タグ」に修正
- 理由: 2026-04-04 のスキーマ変更（throw/counter_hit を始動→タグに移動）への追従漏れ

- 変更内容: `サイト構造設計.md` の starter/tags 選択肢セクションを修正。starter: 5値→4値（投げ/カウンターヒット削除、TL始動追加）。tags: 8値→10値（めくり削除、投げ/カウンターヒット/タッグランチャーまで追加）。各セクションに `コンボ1枚フォーマット.md` への参照を追加
- 理由: 同上。今後は `コンボ1枚フォーマット.md` を唯一の選択肢定義ドキュメントとし、他文書は参照のみとする構造に変更

---

## 2026-04-11（6）

### Sidekick Fuse のタグ交代関連フィルター除外（`src/components/PairComboPage.astro`）

- 始動セレクトから `タッグランチャー始動` を Sidekick 時に非表示
- 展開パネルの「交代」チェックボックスを Sidekick 時に非表示
- タグフィルターの「タッグランチャーまで」チップを Sidekick 時に非表示
- 理由: Sidekick はソロFuseのためタグ交代機能がなく、これらの選択肢を選んでも常に0件になる
- 実装: `isSidekick` フラグは frontmatter で算出済み（`fuseSlug === 'sidekick' | 'sidekick-alt'`）。Astro の SSG 時に静的に切替え
- コンボカードの「交代」バッジはデータ駆動（`combo.tag === true`）のためコード変更不要

### 設計書更新

- `doc/03_詳細設計/コンボカード・フィルタ.md`: 展開パネル仕様・始動セレクト仕様に Sidekick/Juggernaut 差分を追記
- `doc/03_詳細設計/Juggernautページ.md`: Fuse別ページとの差分にタグ交代なし仕様を追記（実装は別途）

---

## 2026-04-11（5）

### ゲージフィルター改修（`src/components/PairComboPage.astro` / `src/scripts/pair-combo-page.ts`）

**選択肢変更（Duo/ALL Fuse）:**
- 変更前: すべて / 使用なし / 使用あり
- 変更後: すべて / 使用なし / 1本以下 / 2本以下
- 理由: 「使用あり」は1本でも5本でも同じ扱いで絞り込みが粗すぎた。上限指定（以下）方式に変更

**Sidekick Fuse の差分対応（`PairComboPage.astro`）:**
- Sidekick（`fuseSlug === 'sidekick' | 'sidekick-alt'`）では Pゲージ上限が5本のため選択肢を拡張: + 3本以下 / 4本以下
- Aゲージセレクトを Sidekick では非表示（ソロFuseのためアシストゲージが存在しない）
- 判定: frontmatter で `isSidekick` を算出し Astro の条件分岐でSSG時に切替え

**フィルターロジック改修（`pair-combo-page.ts` `matches()`）:**
- 変更前: `=== '0'` / `=== '1'` の個別分岐
- 変更後: `state.ult_gauge_point !== '' && card.ultGaugePoint > Number(state.ult_gauge_point)` で統一
- 任意の上限値（0〜4）に対応。値追加時にロジック修正不要

### 設計書更新

- `doc/03_詳細設計/コンボカード・フィルタ.md`: 展開パネルのゲージフィルター仕様をDuo/Sidekick対応表で更新。内部値・動作テーブルを追記
- `doc/03_詳細設計/Juggernautページ.md`: フィルターバー仕様を最新化（始動/位置をセレクト化・Pゲージフィルター追加・Aゲージなし・新着順追加）

---

## 2026-04-11（4）

### データスキーマ（`game_version` フィールド追加）

- 追加: `ComboData.game_version?: string`（`src/data/sample-combos.ts`）
- 任意フィールド。コンボが確認されたパッチバージョン（例: `v0.9.2`）
- サンプルコンボ #1〜4 に `v0.9.1` / `v0.9.2` のサンプル値を追加。#5 は省略（任意フィールドの動作確認）

### コンボカード UI（`src/components/ComboCard.astro`）

- 追加: `.cc-version`（`combo.game_version` がある場合のみ表示）
- 位置: `.cc-header` 内 `.cc-id` の右隣
- スタイル: `font-size: 0.6rem; color: var(--color-text-muted); opacity: 0.5`

### 位置フィルター変更（`src/components/PairComboPage.astro` / `src/scripts/pair-combo-page.ts`）

- 変更: チップ複数選択（中央・端の2値のみ）→ `<select>` 単一選択（位置限定なし + 5値の計6選択肢）
- 理由: `position` フィールドはコンボ1件につき1値（排他）なので複数選択に意味がなかった。また既存チップは5値のうち2値しか表示していなかったため他の位置値が絞り込めない状態だった
- `PairPageState.position`: `Set<string>` → `string`（`''` = すべて）
- `matches()`: `state.position.size > 0 && !state.position.has(card.position)` → `state.position !== '' && state.position !== card.position`

### ソート選択肢に「新着順」追加

- 追加: `id-desc`（新着順）— `id` 降順ソート
- `src/components/PairComboPage.astro`: 並び順セレクトに `<option value="id-desc">新着順</option>` を追加
- `src/scripts/pair-combo-page.ts`: `sortCards()` に `id-desc` ケースを追加

### 設計書更新

- `doc/03_詳細設計/コンボカード・フィルタ.md`: `.cc-version` をヘッダー行仕様・カード構造に追加。位置フィルターをセレクト仕様に更新。新着順をソート選択肢に追加

---

## 2026-04-11（3）

### データスキーマ（`title` フィールド追加）

- 追加: `ComboData.title?: string`（`src/data/sample-combos.ts`）
- 任意フィールド。コンボの短い説明タイトル（15文字以内推奨）
- `notes` との使い分け: `title` は一言ラベル、`notes` は使い方・条件の補足説明
- 理由: コンボ一覧でフィルタ結果を眺めるとき、`inputs` とバッジだけではどのようなコンボか直感的に分からない。タイトルがあれば一覧スキャンの速度が上がる

### コンボカード UI（`src/components/ComboCard.astro`）

- 追加: `.cc-title`（`combo.title` がある場合のみ表示）
- 位置: カード最上部（難易度・ダメージ行より前）
- スタイル: `font-size: 0.85rem; font-weight: 700; color: var(--color-text)`

### 設計書更新

- `doc/02_企画設計/コンボ1枚フォーマット.md`: `title` フィールドを「コンボ内容」セクションに追加。運用方針を記載
- `doc/03_詳細設計/コンボカード・フィルタ.md`: カード構造に `.cc-title` を追加。タイトル行のスタイル仕様を記載

---

## 2026-04-11（2）

### 新規ページ: コンボ単体ページ

- 追加: `/combo/[id]/` を URL 構造に追加（サイト構造設計.md）
- 新規作成: `doc/03_詳細設計/コンボ単体ページ.md`
- 役割:
  - 「このコンボを練習する」ための専用ページ（練習モード）
  - Discord Bot `/combo [ID]` の Web 着地点、SNS 共有用 OGP の着地点
  - 大画面動画 + 番号付き縦リスト入力表記のフォーカス表示
- 理由:
  - Fuse別ページの動画パネルに「探す」と「練習する」の2用途が混在して UI 要件が競合していた
  - フィルタ・一覧が主役の Fuse別ページは「探すモード」に特化し、練習は専用ページに分離することで両方の体験を明確化できる
  - Discord Bot 連携や SNS 共有の着地点として、コンボ単位のページは元々必要だった

### Fuse別ページの動画パネル設計を実装準拠に書き戻し（`doc/03_詳細設計/Fuse別ページ.md`）

- 変更: 動画パネルの入力表記は「番号付き縦リスト（PC・モバイル縦）+ 横書き（モバイル横）」から **「横書き全表記・全レイアウト共通」** に書き戻し
- 理由: 現実装は横書きベースであり、Fuse別ページの動画パネルの役割は「探すモードの軽いチェック」。縦リストは視線移動には有利だが、一覧との共存を優先して軽いパネルに留める方が役割に合う
- 縦リスト + 大画面動画の設計は コンボ単体ページ.md に移動
- PC 2カラムレイアウト（未実装）の役割を明確化: 「動画を見ながら別のコンボも一覧で選べる」探すモードのためのレイアウトであり、練習モードのための大画面表示ではない

### ComboCard に「練習用で開く」リンクを追加（`doc/03_詳細設計/コンボカード・フィルタ.md` / `Fuse別ページ.md`）

- 追加: カードフッターのアクション行に `.cc-detail-link`（`/combo/[id]/` へのリンク）を追加
- 役割: カード上の ▶ 動画ボタンが「探すモードの軽いチェック」を担うのに対し、このリンクは「腰を据えて練習する」導線。コンボ単体ページに遷移する
- 実装は未着手（設計のみ更新）

---

## 2026-04-11

### データスキーマ（`src/data/sample-combos.ts`）

**始動（starter）の変更:**
- 削除: `throw` / `counter_hit`
- 追加: `tag_launcher`
- 理由: throw・counter_hit は排他的な始動状況ではなく、地上/対空と組み合わさる付加条件。タグで表現する方が正確
- combo #5 を `starter: "counter_hit"` → `starter: "ground"` に修正し、tags に `"counter_hit"` を追加

**タグ（tags）の変更:**
- 削除: `crossup`（始動「崩し」と実質的に重複。ユーザーは崩しや notes で検索するため独立タグの価値が薄いと判断）
- 追加: `throw` / `counter_hit` / `to_tag_launcher`

---

### コンボカード（`src/components/ComboCard.astro`）

**starterLabel の変更:**
- 削除: `throw: "投げ"` / `counter_hit: "CH"`
- 追加: `tag_launcher: "TL始動"`

**comboTagLabel の変更:**
- 表示ラベルをカタカナに統一: `fury: "Fury"` → `"フューリー"` / `hkd: "HKD"` → `"ハードノックダウン"` / `limit_strike: "Limit Strike"` → `"リミットストライク"`
- 削除: `crossup`
- 追加: `throw: "投げ"` / `counter_hit: "カウンターヒット"` / `to_tag_launcher: "タッグランチャーまで"`

---

### フィルター UI（`src/components/PairComboPage.astro`）

**パンくず（nav.breadcrumb）の統一:**
- 問題: Fuse別・ペアトップ・キャラ単体ページで構造がバラバラだった
- 解決: `fuseSlug` の有無で2段階/3段階を切替
  - Fuse別: `トップ > キャラA×キャラB > Fuse名`
  - ペアトップ: `トップ > キャラA×キャラB`
- キャラ単体ページへの戻りは `pair-nav-links` に分離（パンくずは canonical 位置を示す、戻り導線は別コンポーネントという関心の分離）

**始動フィルターのUI変更:**
- 変更前: チップ複数選択（`Set<string>`）
- 変更後: `<select>` 単一選択（すべて / 地上 / 対空 / 崩し / タッグランチャー始動）
- 理由: `starter` フィールドはコンボ1件につき1値（排他）なので、複数同時選択に意味がなかった。セレクト式にすることでUIと仕様が一致する

**ゲージフィルターラベルの改善:**
- 変更前: `全て / 0 / 1+`
- 変更後: `すべて / 使用なし / 使用あり`

**タグフィルター選択肢の追加:**
- 変更前: oki / corner_carry / side_switch / hkd / limit_strike（5種）
- 変更後: 全10タグ（fury / counter_hit / throw / hkd / limit_strike / punish / to_tag_launcher を追加）

**セレクト背景色の修正:**
- 問題: `background: transparent` はブラウザが白背景を適用し、ダークモードで白背景に薄い文字色が重なって読めなかった
- 解決: `background: var(--color-surface)` に変更し、ゲージセレクト・始動セレクトで視覚的に統一

---

### フィルターロジック（`src/scripts/pair-combo-page.ts`）

**バグ修正: チップのアクティブ表示が更新されなかった問題:**
- 原因: `renderCards()` の末尾で `syncUiFromState()` が呼ばれていなかった
- 修正: `renderCards()` 内の `syncUrl()` 呼び出し後に `syncUiFromState()` を追加

**starter の型変更（単一選択化に伴う）:**
- `Set<string>` → `string`（`''` = すべて）
- `matches()`: `state.starter.size > 0 && !state.starter.has(card.starter)` → `state.starter !== '' && state.starter !== card.starter`
- セレクト change ハンドラの対象型に `'starter'` を追加

---

### ファイル削除

- `src/pages/ahri/yasuo/[...fuse].astro` を削除
  - 理由: 汎用ルート `src/pages/[char1]/[char2]/[...fuse].astro` で完全に代替可能。二重管理で変更漏れのリスクがあった

---

### 設計ドキュメント

- `doc/02_企画設計/コンボ1枚フォーマット.md` を全面改訂（データ辞書として整備。カード表示/フィルター/自動導出を列で明示）
- `doc/03_詳細設計/コンボカード・フィルタ.md` を改訂（タグ・始動・ゲージフィルター仕様を最新化）
- `doc/03_詳細設計/Fuse別ページ.md` を改訂（フィルターバー・状態管理・パンくず仕様を最新化）
- `doc/実装未定.md` を新規作成（プロジェクト全体の実装未定機能を管理）
- `doc/CHANGELOG.md` を新規作成（本ファイル）
