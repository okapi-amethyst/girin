# コンボカード・フィルタ 詳細設計

> 使用ページ: Fuse別ページ / Juggernautページ（将来: My Combos / 新着コンボ）
> 実装ファイル: `src/components/ComboCard.astro` / `src/scripts/pair-combo-page.ts`（フィルタロジック）
> 最終更新: 2026-04-05

---

## ComboCard

### 構造

```
article.combo-card（border-radius: 8px, bg: surface, cursor: pointer）
├── .cc-header
│   ├── .cc-difficulty（難易度星 + PULSEバッジ）
│   ├── .cc-damage（ダメージ数値）    ← margin-left: auto で右寄せ
│   └── .cc-id（#ID）
├── .cc-badges（状況バッジ群）
├── .cc-inputs-wrap
│   ├── .cc-inputs.cc-inputs-short（短縮入力表記）
│   ├── .cc-inputs.cc-inputs-full（全入力表記, 初期非表示）
│   └── button.cc-expand-btn（「全表記」, 6手以上のみ表示）
├── .cc-tags（追加要素タグ）           ← tags が空なら非表示
├── p.cc-notes（メモ）                ← notes がなければ非表示
└── .cc-footer
    ├── .cc-credit（投稿者名 / 考案者名）
    └── .cc-actions
        ├── リンクコピーボタン
        ├── ▶ 動画ボタン（.cc-video-btn）
        └── ブックマークボタン（disabled）
```

---

### ヘッダー行（`.cc-header`）

| 要素 | スタイル | 内容 |
|---|---|---|
| `.cc-stars` | font-size: 0.7rem, color: #fbbf24 | ★★★☆☆ 形式 |
| `.cc-pulse-badge` | 0.5rem uppercase, bg: danger 20%, color: danger | difficulty ≤ 2 のとき表示 |
| `.cc-damage` | 0.85rem, font-weight: 800 | `combo.damage.toLocaleString()` |
| `.cc-id` | 0.65rem, opacity: 0.6 | `#combo.id` |

---

### 状況バッジ（`.cc-badges`）

| バッジ               | 表示条件                         | 色               |
| ----------------- | ---------------------------- | --------------- |
| 始動（starterLabel）  | 常時                           | accent（黄緑）      |
| 位置（positionLabel） | 常時                           | `#60a5fa`（青）    |
| 交代                | `combo.tag === true`         | `#a78bfa`（紫）    |
| Assist            | `combo.assist_used === true` | `#fb923c`（オレンジ） |
| DUO               | `combo.duo_only === true`    | `#f472b6`（ピンク）  |
| P.ゲージ ×n          | `combo.ult_gauge_point > 0`  | `#38bdf8`（水色）   |
| A.ゲージ ×n          | `combo.ult_gauge_assist > 0` | `#38bdf8`（水色）   |

**ラベル変換:**

| 値 | 始動ラベル |
|---|---|
| `ground` | 地上 |
| `anti_air` | 対空 |
| `throw` | 投げ |
| `pressure_mixup` | 崩し |
| `counter_hit` | CH |

| 値 | 位置ラベル |
|---|---|
| `center` | 中央 |
| `near_corner` | 端寄り |
| `corner` | 端 |
| `near_corner_back` | 端寄り(背) |
| `corner_back` | 端(背) |

---

### 入力表記（`.cc-inputs-wrap`）

- `bg: color-bg, border-radius: 6px, padding: 0.5rem 0.7rem`
- `code` タグ: monospace（Fira Code / Cascadia Code / JetBrains Mono）, font-size: 0.85rem, color: #fbbf24
- `white-space: nowrap`（横スクロール）

**短縮ルール（`shortenInputs()`）:**
- 5手以内（`>` 区切りで ≤5）: そのまま全表示
- 6手以上: `先頭2手 > ... > 末尾2手` で短縮 + 「全表記」ボタン
- ボタンクリックで短縮↔全表記トグル

**記法のレンダリング方針:**
- `mash.X` → そのままテキスト表示
- `(X > Y)` 残キャラ入力ブロック → 括弧内をサブカラー（例: #a78bfa 紫）でハイライト表示し、残キャラの入力であることを視覚的に区別する（デュオ専用コンボのみ出現）

---

### 追加要素タグ（`.cc-tags`）

`combo.tags` 配列から表示。`bg: border, color: text-muted, font-size: 0.6rem`

| 値 | 表示ラベル |
|---|---|
| `oki` | 起き攻め |
| `corner_carry` | 端運び |
| `side_switch` | 入替え |
| `fury` | Fury |
| `hkd` | HKD |
| `limit_strike` | Limit Strike |
| `crossup` | 表裏 |
| `punish` | 確反 |

---

### フッター（`.cc-footer`）

**クレジット（`.cc-credit`）:**
- `.cc-submitter`: 投稿者名（0.65rem）
- `.cc-creator`: 考案者名（0.6rem, opacity 0.7）— `creator_name` がある場合のみ

**アクションボタン（`.cc-actions`）:**

| ボタン | 動作 | 状態 |
|---|---|---|
| リンクコピー | `navigator.clipboard.writeText(url#combo-{id})` | 常時有効 |
| ▶ 動画 | `openVideoPanel(card)` を発火 | 常時有効 |
| ブックマーク | 未実装 | `disabled`（opacity 0.4）|

---

### インタラクション

| 操作 | 動作 |
|---|---|
| カード本体クリック（動画未表示） | 動画パネルを開く |
| カード本体クリック（動画表示中） | 動画パネルを閉じる |
| ▶ ボタン | 動画を開く / 同一カードなら閉じる |
| カード内 button/a/input/select クリック | カード本体クリックとして扱わない（イベント伝搬停止）|
| 再生中 | `.playing` クラス付与 → border-color: accent |

---

### `data-*` 属性（フィルタロジック用）

| 属性 | 値 |
|---|---|
| `data-id` | combo.id |
| `data-difficulty` | 1〜5 |
| `data-starter` | ground / anti_air / throw / pressure_mixup / counter_hit |
| `data-position` | center / corner / near_corner / etc. |
| `data-assist-used` | true / false |
| `data-tag` | true / false |
| `data-duo-only` | true / false |
| `data-ult-gauge-point` | 0〜 |
| `data-ult-gauge-assist` | 0〜 |
| `data-tags` | カンマ区切り文字列 |
| `data-practicality` | 数値 |
| `data-damage` | 数値 |

---

## フィルターバー（共通仕様）

### 常時表示行

| フィルター | UI | 備考 |
|---|---|---|
| 難易度 | チップ ★1〜★5 | ★1/★2 は PULSE バッジ付き |
| 始動 | チップ | 複数選択可（OR）|
| 位置 | チップ | 複数選択可（OR）|
| 並び順 | `<select>` | 詳細は下記 |
| その他 ▼ | トグルボタン | Fuse別のみ（展開パネルあり）|

**チップスタイル:**
- 通常: `border: border, color: text-muted, bg: transparent`
- アクティブ: `border/color: accent, bg: accent 10% mix`
- hover: `border: accent, color: text`

**ソート選択肢（Fuse別 / Juggernaut で若干異なる）:**

| 値 | Fuse別ラベル | Juggernautラベル |
|---|---|---|
| `practicality-desc` | 実用度順 | 実用度 |
| `damage-desc` | ダメージ順 | ダメージ（高い順）|
| `difficulty-asc` | 難易度順（易しい順）| 難易度（低い順）|
| `difficulty-desc` | 難易度順（難しい順）| —（Juggernautにはなし）|

### 展開パネル（Fuse別のみ）

| フィルター | UI |
|---|---|
| アシスト使用 | checkbox |
| 交代 | checkbox |
| DUO限定 | checkbox |
| Pゲージ | `<select>`（全て / 0 / 1+）|
| Aゲージ | `<select>`（全て / 0 / 1+）|
| タグ | チップ複数選択（OR）|

---

## フィルタロジック（`pair-combo-page.ts`）

### マッチング条件（`matches()` 関数）

各フィルタは AND 結合。複数選択のチップは OR 結合。

```
難易度: state.difficulty に card.difficulty が含まれる（複数選択OR）
始動:   state.starter に card.starter が含まれる（複数選択OR）
位置:   state.position に card.position が含まれる（複数選択OR）
タグ:   state.tags のいずれかが card.tags に含まれる（OR）
アシスト: state.assist_used が true なら card.assistUsed も true
交代:   state.tag が true なら card.tag も true
DUO:    state.duo_only が true なら card.duoOnly も true
Pゲージ: '0' → 0のみ, '1' → 1以上
Aゲージ: 同上
```

### ソート（`sortCards()` 関数）

| sort 値 | 第1キー | 第2キー |
|---|---|---|
| `practicality-desc` | practicality 降順 | damage 降順 |
| `damage-desc` | damage 降順 | id 昇順 |
| `difficulty-asc` | difficulty 昇順 | practicality 降順 |
| `difficulty-desc` | difficulty 降順 | damage 降順 |

### URL クエリ同期

- `history.replaceState` でフィルタ状態をURLに反映
- デフォルト値はクエリに含めない（`practicality-desc`・`page=1` は省略）
- Fuseタブのリンク href にも現在のクエリを引き継ぐ（タブ切替後もフィルタ維持）
- ページ読み込み時に `applyQueryState()` でURLから状態を復元

---

## 課題・検討事項

### Juggernaut のフィルタ未実装

- チップのトグルは動くが `renderCards()` 相当のロジックがなく実際の絞り込みは未実装
- 実データ投入時に `initPairComboPage()` 相当を適用する

### フィルター全クリアボタン

- 現状、個別フィルタを一括解除する手段がない
- 全クリアボタンを filter-bar に追加することを検討

### タグフィルターの選択肢不足

- `fury`（フューリー使用）/ `crossup`（めくり）/ `punish`（確反）はデータ上存在するがフィルター UI には含まれていない

### ブックマーク機能

- `.cc-bookmark-btn` は `disabled` プレースホルダー
- Phase 2: ローカルストレージ保存 + My Combos ページ連携

### ComboCard の再利用先

- 現状は Fuse別ページのみで使用
- My Combos / 新着コンボ ページでも同コンポーネントを使う想定
