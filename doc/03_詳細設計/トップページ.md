# トップページ 詳細設計

> 対象URL: `/`
> 実装ファイル: `src/pages/index.astro`
> 最終更新: 2026-04-12

---

## ページ概要

サイトへの入口。キャラクターを選択してコンボを探す起点となるページ。
また新着コンボ・My Combos・コンボ投稿への機能導線カードも設置する。

---

## コンポーネント構成

```
index.astro
└── Base.astro（ヘッダー・フッター）
    ├── section.hero              ← キャッチコピー
    ├── section.nav-links         ← 機能導線カード
    │   └── .nav-links-row（flex, 中央寄せ）
    │       ├── .nav-link-card（新着コンボ）
    │       ├── .nav-link-card（My Combos）
    │       └── .nav-link-card（コンボ投稿）
    └── section.characters        ← キャラ選択グリッド
        ├── h2.char-grid-title（「コンボを調べたいチャンピオンを選択」）
        └── .char-grid
            └── a.char-card × n
                ├── .char-portrait（キャラ画像）
                └── .char-label（キャラ名）
```

---

## 各コンポーネント詳細

### ヒーローエリア（`section.hero`）

```
今のあなたに最適なコンボがみつかるかも
```

- テキストのみ（font-size: 0.95rem, color: text-muted）
- padding: 1.5rem 0 1rem
- **課題:** サイトの目的・ゲーム名を伝えるキャッチコピーとして弱い。要検討

---

### 機能導線カード（`section.nav-links`）

| カード | サブ説明文 | 状態 | リンク先 |
|---|---|---|---|
| 新着コンボ | 新しいコンボをみる | 未実装（`pointer-events: none`, opacity 0.6, label に取り消し線） | `#` |
| My Combos | 保存したコンボをみる | 未実装（同上） | `/mycombo/` |
| コンボ投稿 | コンボを共有する | 未実装（同上） | `#` |

- カードスタイル: `background: surface, border: border, border-radius: 8px, flex: 1, min-width: 0, padding: 0.45rem 0.75rem`
- ラベル（font-weight: 700, `text-decoration: line-through`）+ サブ説明文（0.7rem muted）の縦2行構成
- ~~Coming Soon バッジ~~ → 廃止。ラベルの取り消し線で未実装を表現（2026-04-08）
- **レスポンシブ:** `flex: 1` により3枚が横幅を等分。375px（モバイル最小幅）でも1行に収まる
- **文言方針:** ヘッダーナビ（新着コンボ / My Combos / コンボ投稿）と表記を統一する

---

### キャラ選択グリッド（`section.characters`）

グリッド直上に `h2.char-grid-title`「コンボを調べたいチャンピオンを選択」を配置（1rem, font-weight: 700）。

**グリッドカラム数（レスポンシブ）:**

| ブレークポイント | カラム数 | gap |
|---|---|---|
| ～639px | 3列 | 0.5rem |
| 640px～ | 4列 | 0.75rem |
| 960px～ | **6列** | 0.75rem |

> PC 6列・13キャラの場合: 2行（6+6）+ 最終行1枚。14キャラ追加後は2行（6+6）+ 最終行2枚。

**キャラカード（`a.char-card`）:**

| 要素 | 内容 |
|---|---|
| `.char-portrait` | キャラ画像（aspect-ratio: 6/5, background-image でクロップ）|
| フォールバック | 画像なし時: 名前の頭文字 |
| `.char-label` | キャラ名（0.8rem, overflow: ellipsis）|
| ホバー | border-color → accent |

- リンク先: `/[char.slug]/`
- 画像スケール: JS で `data-size` / `data-pos-x` / `data-pos-y` を元に `background-size/position` を動的計算（WIKI_REF: 180px 基準）

---

## OGP / SEO

### ページタイトル
```
2XKO GIRIN — コンボガイド
```

### メタディスクリプション
```
2XKOのコンボをキャラ・Fuse・難易度で絞り込めるコンボガイド。初心者から上級者まで使えるコンボを掲載。
```

### OG画像
- 共通画像（GIRINロゴ + サイトキャッチコピー）
- サイズ: 800×418

### canonical
- `/`

---

## 広告配置（要検討 / Phase 2〜）

> AdSense 等のバナー広告を Phase 2 から導入する想定。以下は「邪魔にならない位置」の候補案。
> 実装時は実際のレイアウトと照らし合わせて最終決定すること。

### PC サイドバー広告（実装済み・Phase 2 で有効化）

viewport 1360px 以上で、コンテンツ（960px）の左右両外側に固定サイドバー広告スロットを配置済み。

```
[左広告 160px] ← 36px gap → [コンテンツ 960px] ← 36px gap → [右広告 160px]
                     合計 min-width: 1360px で表示
```

- 実装: `Base.astro` に `<aside class="ad-sidebar-left/right">` を `position: fixed` で配置
- Phase 1 では `.ad-placeholder`（破線枠+「広告 160×600 Phase 2〜」）を表示
- Phase 2 移行時: `.ad-placeholder` を AdSense タグに差し替えるだけ
- z-index: 10（headroom-nav: 200 より低い）
- **左サイドバーは将来のナビゲーション/メニュー転用も想定**

### コンテンツ内広告（実装済み・Phase 2 で有効化）

キャラグリッドの下に **336×280（ラージレクタングル）** プレースホルダーを実装済み。

```
キャラグリッド
  ↓
[336×280 広告] ← 中央寄せ（margin: auto）
  ↓
フッター
```

- 実装: `section.characters` 内 `.char-grid` 直後に `.ad-inline-placeholder` を配置
- 中央寄せ: `margin-left: auto; margin-right: auto`
- **PC 1360px 以上ではサイドバー広告が表示されるため `display: none` で非表示**
- Phase 2 移行時: `.ad-inline-placeholder` を AdSense `336×280` タグに差し替える

### その他候補位置（未実装・要検討）

| # | 位置 | セクション間の区切り |
|---|---|---|
| ① | `section.nav-links` と `section.characters` の間 | 機能導線カード ↓ **[広告]** ↓ キャラグリッド |

- ①はファーストビュー直後のため UX への影響が大きい。現状は見送り。

### 避けるべき位置

- **ヒーローエリア内・前後**: ファーストビューで広告が目立つとブランドイメージを損なう
- **キャラグリッド内に混入**: キャラカードと見分けがつかず誤タップを誘発する

---

## 課題・検討事項

### ヒーローエリアのコピー

- 現状のキャッチコピーはプレースホルダー的な内容
- 「2XKO コンボガイド」「ペア × Fuse でコンボを探す」など、サイトの目的が伝わる文言に変更したい

### 最近追加されたコンボ（未実装）

- 設計上はトップに新着コンボを数件表示する予定
- Phase 1 では DB 連携がないため静的データまたはモックで対応するか、省略するかの方針が未定

### キャラカードの情報量

- 現状は画像 + キャラ名のみ
- コンボ登録件数などのバッジを出す案もあるが、Phase 1 では未実装

### グリッドの最大幅

- PC 6列・13キャラで確認済み（2行 + 最終行1枚）。問題なし。
- ~~`.container` の `max-width: 960px` に収まっているが、13〜14キャラでの見た目確認が必要~~（解決）
- **補足:** `Base.astro` の CSS が `<style is:global>` でないと `<slot>` 内の `.container` に max-width が効かないバグを修正済み（2026-04-08）
