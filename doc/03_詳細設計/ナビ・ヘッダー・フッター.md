# ナビ・ヘッダー・フッター 詳細設計

> 実装ファイル: `src/layouts/Base.astro`（Headroom は各ページコンポーネントに実装）
> 最終更新: 2026-04-12

---

## 全体構造

```
Base.astro
├── <head>                    ← メタ情報・OGP・テーマ初期化スクリプト
├── <body>
│   ├── header.site-header    ← グローバルヘッダー
│   ├── main > <slot />       ← 各ページのコンテンツ
│   └── footer.site-footer    ← グローバルフッター
└── <script>                  ← テーマ切替・シェアボタン
```

---

## `<head>`

| 要素 | 内容 |
|---|---|
| charset | UTF-8 |
| favicon | `/favicon.svg` |
| viewport | width=device-width, initial-scale=1 |
| description | ページ固有 or デフォルト `"2XKO GIRIN — これだけ覚えたら対戦行ってOK"` |
| og:title | ページタイトル |
| og:description | ページ説明 |
| og:image | ページ固有 or デフォルト `/og-default.png` |
| og:type | `website` |
| og:url | 現在の URL（`Astro.url`）|
| twitter:card | `summary_large_image` |
| title | ページタイトル |

**テーマ初期化（`is:inline` スクリプト）:**
- `localStorage.getItem('theme')` があれば `data-theme` に適用
- なければ `prefers-color-scheme: light` に応じて自動設定
- ページ描画前に実行（FOUC防止）

---

## ヘッダー（`header.site-header`）

```
[ GIRIN ]  [ 2XKO ]                    （右寄せ）  [ ~~新着~~ ]  [ ~~My Combos~~ ]  [ ~~コンボ投稿~~ ]  [ Search(disabled) ]  [ ☀/☾ ]
```

### レイアウト

- ロゴ・2XKO バッジが左端
- `.header-nav-start`（新着）に `margin-left: auto` を設定することで、以降の要素が右寄せになる

### 要素一覧

| 要素 | クラス | 内容 | 状態 |
|---|---|---|---|
| ロゴ | `.site-logo` | GIRIN テキスト | トップへリンク |
| ゲームバッジ | `.site-badge` | `2XKO` | accent背景, 非リンク |
| 新着 | `span.header-link.is-disabled.header-nav-start` | 取り消し線テキスト | **リンクなし**（`<span><s>`）。Phase 2 で `<a>` に変更 |
| My Combos | `span.header-link.is-disabled` | 取り消し線テキスト | 同上 |
| コンボ投稿 | `span.header-link.is-disabled` | 取り消し線テキスト | 同上 |
| Search | `.header-icon-btn[disabled]` | 将来の検索用プレースホルダー | disabled（opacity 0.45）|
| テーマ切替 | `.theme-toggle` | ☀（ライト時）/ ☾（ダーク時）| 右端 |

**Phase 1 の方針:** 新着・My Combos・コンボ投稿は `<span><s>` による取り消し線テキストで配置。Coming Soon バッジは使用しない（スペース節約・見た目検証の邪魔にならないため）。Phase 2 でページ実装時に `<a href="...">` に差し替える。

**Share ボタンは削除済み（Phase 1 では不要）。** 必要になった場合は各ページ内に個別ボタンとして配置する想定。

### スタイル

- `border-bottom: 1px solid var(--color-border)`, `padding: 0.75rem 0`
- コンテナ内: `display: flex, align-items: center, gap: 0.75rem, flex-wrap: wrap`
- `.site-logo`: font-size 1.1rem, font-weight 700
- `.site-badge`: accent 背景, `#0a0a0f` 文字, 0.7rem uppercase

### Share ボタンの動作

1. `navigator.clipboard.writeText(window.location.href)` でURL全体をコピー
2. 成功: 「コピーしました」を 1.8 秒表示
3. 失敗: 「コピーに失敗しました」を表示

### テーマ切替の動作

- クリックで `dark` ↔ `light` をトグル
- `data-theme` 属性を `<html>` に付与 + `localStorage` に保存
- CSS: `dark` 時は `.theme-icon-light` を非表示、`light` 時は `.theme-icon-dark` を非表示

---

## フッター（`footer.site-footer`）

```
© 2026 GIRIN | 2XKO GIRIN はファンサイトであり Riot Games とは無関係です
2XKO GIRIN is not endorsed by Riot Games...（英文権利表記）
[ 免責事項（展開） ]  [ プライバシーポリシー ]  [ Discord サーバーに参加 ]  [ 𝕏 ]
```

### 要素一覧

| 要素 | 内容 |
|---|---|
| `.footer-copy` | `© 2026 GIRIN | …` 日本語コピーライト |
| `.footer-note` | 英文権利表記 |
| `.footer-details` | `<details>` 展開式免責事項 |
| プライバシーポリシー | `/privacy/` へのリンク |
| Discord サーバーリンク | 準備でき次第差し替え（下記「リンク管理」参照）|
| Discord Bot 招待リンク | 同上 |
| X リンク | 同上 |

### リンク管理（要記載）

実装ファイル（`Base.astro`）上はプレースホルダー URL で場所を確保しておく。
各リンクが用意できた時点でここに記載し、実装に反映する。

| リンク | URL | 状態 |
|---|---|---|
| Discordサーバー招待 | ― | **未作成** |
| Discord Bot 招待 | ― | **未作成** |
| X（Twitter）アカウント | ― | **未作成** |

### スタイル

- `border-top: 1px solid var(--color-border)`, `padding: 2rem 0`, `margin-top: 3rem`
- `color: text-muted, font-size: 0.85rem`
- `.footer-links`: `flex-wrap: wrap, gap: 0.85rem 1.2rem`
- ソーシャルアイコン: 24px 丸ボーダー内にシンボル文字

---

## パンくずナビ（各ページ実装）

Base.astro には含まれず各ページで独自実装。パターンは以下の3種:

| ページ | パンくず |
|---|---|
| キャラ単体 | トップ / キャラ名（現在地）|
| ペアトップ | トップ / キャラA×キャラB（現在地）|
| Fuse別 | トップ / キャラA×キャラB / Fuse名（現在地）|
| Juggernaut | トップ / キャラ名 / JUGGERNAUT（現在地）|

Fuse別とJuggernautでパンくずの段数が異なるのは意図的（親ページの構造が違うため）。詳細な設計方針は `Fuse別ページ.md` の「パンくずナビ」セクションを参照。

---

## Headroom ナビ（全ページ共通）

スクロールダウン時、ページ固有のランドマーク（フィルターバーやページヘッダー等）が画面外に出たときに表示される固定ナビ。現時点の最新実装は Fuse別ページ（`PairComboPage.astro`）であり、スタイル・構造はそれを標準とする。

**ページ別のトリガー条件と表示内容は各ページの設計書に定義する。** トップ・キャラ単体など縦の高さが出にくいページでは実質ほぼ表示されないが、全ページに実装しておく。

### 構造

```
div.headroom-nav（position: fixed, full-width）
  └── div.headroom-nav-inner（max-width: 960px, padding: 0 1rem, 中央寄せ）
      └── a.headroom-nav-link（ペア名 + Fuseラベル）
```

### コンテンツ左端の揃え方針

**`headroom-nav-inner` は `.container.page` と同じ `max-width: 960px; margin: 0 auto; padding: 0 1rem` を使う。** これにより、リンクテキストの左端がコンボカードの左端と一致する。

- 外側の `.headroom-nav` は `left: 0; right: 0` で viewport 全幅を占有
- 内側の `.headroom-nav-inner` がコンテナ幅制約とパディングを担う
- 上下の余白（`padding: 0.4rem 0`）は外側ではなく内側で持つ

この方針は全ページ共通で適用する。

### スタイル

| クラス | 主なプロパティ |
|---|---|
| `.headroom-nav` | `position: fixed; top: 0; left: 0; right: 0; z-index: 200; bg: color-bg; border-bottom: 1px solid border` |
| `.headroom-nav-inner` | `max-width: 960px; margin: 0 auto; padding: 0.4rem 1rem; display: flex; align-items: center` |
| `.headroom-nav-link` | `font-size: 0.8rem; font-weight: 700; color: text` |
| `.headroom-nav-fuse` | `font-size: 0.65rem; color: accent; uppercase` |

### 表示/非表示

- 非表示: `transform: translateY(-100%); pointer-events: none`
- 表示: `.visible` クラス付与 → `transform: translateY(0); pointer-events: auto`
- 動画パネルの `top` 値と連動（ページ側で 34px オフセット）

---

## 課題・検討事項

### ヘッダーのレスポンシブ

- 現状 `flex-wrap: wrap` で対応しているが、スマホ幅でアイテムが折り返す可能性がある
- 将来の機能追加でアイテムが増えた場合はハンバーガーメニューへの格納を検討（設計書記載済み）

### Share ボタンのテキスト

- 現状「Share」テキストのみ。アイコン化する案がある

### Search プレースホルダー

- 将来のサイト内検索用に場所を確保しているだけで機能なし
- Phase 1 は disabled のまま

